# Verschoben

Die optionalen Schriftdateien liegen jetzt in `src/assets/fonts/` — dort hasht
Vite sie beim Build und schreibt relative Pfade, sodass die Seite auch in einem
Unterordner läuft (GitHub Pages, Testverzeichnis).

Anleitung: `src/assets/fonts/README.md`.

Dieser Ordner kann gelöscht werden.
